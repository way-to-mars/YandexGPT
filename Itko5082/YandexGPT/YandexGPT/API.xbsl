/**
 * Здесь находятся функции публичного API
*/

/*
 * OAuth-токен необходим для авторизации в Yandex Cloud пользователя с аккаунтом на Яндексе: пользователь обменивает OAuth-токен на IAM-токен.
 * Срок жизни OAuth-токена — 1 год. После этого необходимо получить новый OAuth-токен и повторить процедуру аутентификации.
 * 
 * OAuth-токен можно отозвать. При этом IAM-токены, для получения которых использовался OAuth-токен, продолжат действовать.
 * Поэтому все такие IAM-токены также должны быть отозваны.
 * (https://yandex.cloud/ru/docs/iam/operations/compromised-credentials#oauth-reissue)
 * 

*/

@Глобально @НаСервере @ДоступноСКлиента
метод ИмяМетода1(): ничто
    выбросить новый ИсключениеНедопустимоеСостояние("Метод не реализован")    
;

@Глобально @НаСервере @ДоступноСКлиента
метод ИмяМетода2(): ничто
    выбросить новый ИсключениеНедопустимоеСостояние("Метод не реализован")
;

@Глобально @НаСервере @ДоступноСКлиента
метод ИмяМетода3(): ничто
    выбросить новый ИсключениеНедопустимоеСостояние("Метод не реализован")
;

@Глобально @НаСервере @ДоступноСКлиента
метод ИмяМетода4(): ничто
    выбросить новый ИсключениеНедопустимоеСостояние("Метод не реализован")
;